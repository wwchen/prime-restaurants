<h1>Listing yelp_infos</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @yelp_infos.each do |yelp_info| %>
  <tr>
    <td><%= link_to 'Show', yelp_info %></td>
    <td><%= link_to 'Edit', edit_yelp_info_path(yelp_info) %></td>
    <td><%= link_to 'Destroy', yelp_info, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Yelp info', new_yelp_info_path %>

<%= form_for(@yelp_category) do |f| %>
  <% if @yelp_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@yelp_category.errors.count, "error") %> prohibited this yelp_category from being saved:</h2>

      <ul>
      <% @yelp_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
